import test from 'node:test';import assert from 'node:assert/strict';
import {mkdtemp,readFile,writeFile,readdir,rm} from 'node:fs/promises';import {tmpdir} from 'node:os';import {join} from 'node:path';import {createHash} from 'node:crypto';
import {downloadVerified,allowedAssetURL,assetResponse} from '../desktop/download.mjs';import {validateManifest,trustedService} from '../shared/manifest.mjs';import {inspectExe} from '../desktop/publish.mjs';
const payload=Buffer.alloc(256);payload.write('MZ');payload.writeUInt32LE(128,60);payload.write('PE\0\0',128);payload.writeUInt16LE(0x8664,132);
const manifest={id:'test-app',name:'Test App',description:'A test executable fixture that is never run.',kind:'app',version:'1.0.0',publisher:'tester',url:'https://github.com/tester/apps/releases/download/v1/test.exe',sha256:createHash('sha256').update(payload).digest('hex'),size:payload.length,license:'MIT'};
async function directory(t){const dir=await mkdtemp(join(tmpdir(),'nutty-tests-'));t.after(()=>rm(dir,{recursive:true,force:true}));return dir;}
test('valid manifest is normalized and unknown properties are stripped',()=>{const m=validateManifest({...manifest,admin:true});assert.equal(m.admin,undefined);assert.equal(m.id,'test-app');});
test('rejects unsafe IDs, hashes, sizes, categories, and release URLs',()=>{for(const patch of [{id:'../escape'},{sha256:'bad'},{size:0},{size:2**32},{kind:'script'},{url:'http://github.com/u/r/releases/download/v1/x.exe'},{url:'https://github.com.evil.test/u/r/releases/download/v1/x.exe'},{url:'https://github.com/u/r/releases/download/v1/x.exe?anything=1'},{url:'https://u:pass@github.com/u/r/releases/download/v1/x.exe'}])assert.throws(()=>validateManifest({...manifest,...patch}));});
test('only HTTPS services and local HTTP origins are accepted',()=>{assert.equal(trustedService('http://127.0.0.1:5080'),'http://127.0.0.1:5080');assert.equal(trustedService('https://example.com'),'https://example.com');for(const url of ['http://example.com','https://user:pass@example.com','file:///tmp','https://example.com/api'])assert.throws(()=>trustedService(url));});
test('verifies bytes, writes final file, and reuses verified cache',async t=>{const dir=await directory(t);let calls=0;const fetcher=async()=>{calls++;return new Response(payload,{status:200,headers:{'content-length':String(payload.length)}});};const path=await downloadVerified(manifest,dir,{fetcher});assert.deepEqual(await readFile(path),payload);assert.equal(await downloadVerified(manifest,dir,{fetcher}),path);assert.equal(calls,1);assert.deepEqual(await readdir(dir),[path.split(/[\\/]/).at(-1)]);});
test('corrupt cache is replaced by newly verified bytes',async t=>{const dir=await directory(t);const options={fetcher:async()=>new Response(payload)};const path=await downloadVerified(manifest,dir,options);await writeFile(path,Buffer.alloc(payload.length));await downloadVerified(manifest,dir,options);assert.deepEqual(await readFile(path),payload);});
test('hash mismatch leaves no partial or executable file',async t=>{const dir=await directory(t);await assert.rejects(downloadVerified(manifest,dir,{fetcher:async()=>new Response(Buffer.alloc(payload.length))}),/Verification failed/);assert.deepEqual(await readdir(dir),[]);});
test('truncated transfers leave no file',async t=>{const dir=await directory(t);await assert.rejects(downloadVerified(manifest,dir,{fetcher:async()=>new Response(payload.subarray(0,100))}),/Verification failed/);assert.deepEqual(await readdir(dir),[]);});
test('oversized transfers are discarded',async t=>{const dir=await directory(t);await assert.rejects(downloadVerified(manifest,dir,{fetcher:async()=>new Response(Buffer.alloc(300))}),/exceeded/);assert.deepEqual(await readdir(dir),[]);});
test('false content length is rejected before saving',async t=>{const dir=await directory(t);await assert.rejects(downloadVerified(manifest,dir,{fetcher:async()=>new Response(payload,{headers:{'content-length':'900'}})}),/size differs/);assert.deepEqual(await readdir(dir),[]);});
test('aborted download is cleaned up',async t=>{const dir=await directory(t);const controller=new AbortController();controller.abort();await assert.rejects(downloadVerified(manifest,dir,{signal:controller.signal,fetcher:async()=>new Response(payload)}),/cancelled/);assert.deepEqual(await readdir(dir),[]);});
test('redirects cannot reach local or unrelated hosts',async()=>{let calls=0;await assert.rejects(assetResponse(manifest.url,undefined,async()=>{calls++;return new Response(null,{status:302,headers:{location:'http://127.0.0.1/private'}});}),/untrusted/);assert.equal(calls,1);for(const value of ['https://evil.test/x','http://github.com/x','https://user:pass@github.com/x'])assert.throws(()=>allowedAssetURL(value));});
test('GitHub asset redirects are supported',async()=>{let calls=0;const r=await assetResponse(manifest.url,undefined,async()=>++calls===1?new Response(null,{status:302,headers:{location:'https://release-assets.githubusercontent.com/signed.exe?token=temporary'}}):new Response(payload));assert.equal(calls,2);assert.deepEqual(Buffer.from(await r.arrayBuffer()),payload);});
test('PE inspection validates headers and computes actual hash',async t=>{const dir=await directory(t);const file=join(dir,'fixture.exe');await writeFile(file,payload);assert.deepEqual(await inspectExe(file),{size:manifest.size,sha256:manifest.sha256});});
test('PE inspection rejects renamed text and bad offsets',async t=>{const dir=await directory(t);const file=join(dir,'fixture.exe');await writeFile(file,Buffer.alloc(256));await assert.rejects(inspectExe(file),/header/);const broken=Buffer.from(payload);broken.writeUInt32LE(99999,60);await writeFile(file,broken);await assert.rejects(inspectExe(file),/offset/);});
